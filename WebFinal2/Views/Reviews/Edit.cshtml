@model WebFinal2.Models.Review

@{
    ViewBag.Title = "Редактировать отзыв";
}

<div class="container">
    <div class="wrapper">
        <div class="col-xs-6 reviewSection">
            @if (User.Identity.IsAuthenticated)
            {
                <h2 class="headline about-header">Редактировать отзыв</h2>
                using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.ID)

                        <div class="form-group">
                            <div class="col-md-10">
                                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = " re-call-input" } })
                                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-md-10">
                                @Html.EditorFor(model => model.Text, new { htmlAttributes = new { @class = "textarea" } })
                                @Html.ValidationMessageFor(model => model.Text, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="form-group loginButton">
                            <div class="col-md-offset-2 col-md-10">
                                <input type="submit" value="СОХРАНИТЬ" class="input send order_text" />
                            </div>
                        </div>
                    </div>
                }

                <button type="button" class="btn btn-primary backbutton">
                    <p class="order_text">
                        @Html.ActionLink("Назад", "Index")
                    </p>
                </button>
            }
            else
            {
                <h2 class="headline about-header">Вы не вошли в аккаунт!</h2>
                <button type="button" class="order btn btn-primary">
                    <p class="order_text">  @Html.ActionLink("ВОЙТИ", "Login", "Accounts", null, new { @class = "actionlink" })</p>
                </button>
            }

            @section Scripts {
                @Scripts.Render("~/bundles/jqueryval")
            } 

        </div>
    </div>
</div>
